import { createTheme } from '@mui/material/styles';

/* Create a Bank of America style application */
export const theme = createTheme({
  palette: {
    primary: {
      main: "#0065B3",
    },
    secondary: {
      main: "#E61030",
    },
  },
});